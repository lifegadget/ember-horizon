import logger from 'dummy/utils/logger';
import { module, test } from 'qunit';

module('Unit | Utility | logger');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = logger();
  assert.ok(result);
});
